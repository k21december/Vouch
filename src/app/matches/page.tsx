import MatchesScreen from "@/features/matches/screen";

export const metadata = {
  title: "Matches | Vouch",
  description: "Your mutual matches",
};

export default function Page() {
  return <MatchesScreen />;
}
